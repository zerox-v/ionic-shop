import { View,Text } from '@tarojs/components';
import  React from 'react';

interface IShopProps {
}

const Shop: React.FunctionComponent<IShopProps> = (props) => {
  return <View className="p24">

    <View className="card">
      <Text/>
    </View>
  </View>;
};

export default Shop;
